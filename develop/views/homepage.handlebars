<div class="jumbotron">
    <h1>ShapeShift</h1>
</div>
<div class="container-fluid text-center">
    <div class="row content">
        <div class="col-sm-2 sidenav">
            <div class="side">
                <div>
                    <a href="/workout/{{workout[0].workout.id}}">
                        <p> {{format_date workout[0].Workout_date}}</p>
                        <p>{{workout[0].workout.details}}</p>
                    </a>
                </div>
                <div>
                  <a href="/workout/{{workout.workout[1].id}}">
                        <p> {{format_date workout[1].Workout_date}}</p>
                        <p> {{workout[1].workout.details}}</p>
                    </a>
                </div>
                <div>
                 <a href="/workout/{{workout[2].workout.id}}">
                        <p> {{format_date workout[2].Workout_date}}</p>
                        <p> {{workout[2].workout.details}}</p>
                    </a>
                </div>

            </div>

        </div>
        <div class="container-fluid text-center">
            <div class="row content">
                <div class="col-sm-8 text-center">
                    <h1>ShapeShift</h1>
                    <h3>Have YOU been coding and havent left your chair all day?</h3>
                    <h3>Wanting to change your shape?</h3>
                    <h3>ShapeShift is for you!</h3>
                    <button type="button" class="btn btn-default">Get Started</button>

                    <h2>Random blog posts</h2>
                    <div class="post">
                        <h3>{{blog.title}}</h3>
                        <p>{{blog.description}}</p>

                    </div>
                </div>


            </div>
        </div>
    </div>
</div>

<footer class="container-fluid text-center">
    <p>ShapeShifters </p>
</footer>

</body>